# mock (default, works offline, no server) | synthesia (needs /server running)
VITE_AVATAR_PROVIDER=mock
# API base for the browser. Leave empty for same-origin (Vite dev proxies /api -> API_PROXY_TARGET,
# default http://localhost:8787). Set an absolute URL only if the server is on another origin.
VITE_API_BASE_URL=
